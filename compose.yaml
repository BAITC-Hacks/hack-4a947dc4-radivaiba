services:
  careerquest:
    build: .
    ports:
      - '127.0.0.1:8080:8080'
    env_file: .env
    environment:
      HOST: 0.0.0.0
      PORT: 8080
    volumes:
      - ./data/seed:/app/data/seed:ro
      - progress:/app/data/runtime
    restart: unless-stopped
volumes:
  progress:
